security:
    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    encoders:
        App\Entity\User:
            algorithm: auto

    providers:
        #in_memory: { memory: null }
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:

            anonymous: lazy
            guard:
                authenticators:
                    - App\Security\AppCustomAuthenticator
            json_login:
                check_path: app_login
                username_path: email
                password_path: password

            logout:
                path: app_logout
                # where to redirect after logout
                # target: app_any_route

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used

    access_control:
        - { path: ^/api, roles: IS_AUTHENTICATED_ANONYMOUSLY }
          #- { path: ^/admin, roles: ROLE_ADMIN }
          #- { path: ^/public, roles: IS_.... }


          #www.wewine.com > ./././front/

          #api.wewine.com  > ./././back/
          #api.wewine.com/public/
          #   api.wewine.com/app/
          #   api.wewine.com/admin/


          #www.wewine.com/ > ./././front/

          #www.wewine.com/api/ > ./././back/
          #www.wewine.com/api/public/
          #www.wewine.com/api/app/
          #www.wewine.com/api/admin/






          #http://www.wewine.com/api/index.php/login
      #http://www.wewine.com/api/login




#"status": "error",
#"error_code": 1,
#"error_msg": "WTF"
#"data": {  }














